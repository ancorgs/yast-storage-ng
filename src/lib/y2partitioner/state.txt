current
-------
initialization
  -> always to the first disk

active_tab
  -> 
selected_row
  -> In System, when deleting an LV, it doesn't go back to its VG

  -> When creating a btrfs in its corresponding section, not selected

  -> Select a device page. The remembered row is selected. Ok.
     Go to "Used Devices". The remembered row is selected. Ok.
     Go back to "Overview" of the same page. The first row is selected. Wrong.
     Visit another page and come back to this. The remembered row is selected.

  -> In system, edit used devices, then add a LV. The new LV doesn't get selected.
     Not always reproducible. Not even sure it's true :-(

